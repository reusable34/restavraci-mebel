#!/bin/bash

# Скрипт для обновления проекта на сервере через git
# Выполните на сервере: bash update-on-server.sh

set -e

APP_DIR="/opt/restavraci"
SERVICE_NAME="restavraci.service"

echo "🔄 Обновляю проект из git репозитория..."

cd "$APP_DIR"

# Проверка наличия git репозитория
if [ ! -d ".git" ]; then
    echo "❌ Git репозиторий не найден!"
    echo "💡 Нужно сначала клонировать репозиторий или инициализировать git"
    exit 1
fi

# Получаем последние изменения
echo "📥 Получаю изменения из репозитория..."
git pull

# Устанавливаем зависимости (если package.json изменился)
echo "📦 Проверяю зависимости..."
npm ci

# Собираем проект
echo "🔨 Собираю Next.js приложение..."
npm run build

# Перезапускаем сервис
echo "🔄 Перезапускаю сервис..."
systemctl restart "$SERVICE_NAME"

# Проверка статуса
sleep 2
if systemctl is-active --quiet "$SERVICE_NAME"; then
    echo "✅ Обновление завершено успешно!"
    echo "📊 Статус сервиса:"
    systemctl status "$SERVICE_NAME" --no-pager -l | head -10
else
    echo "❌ Ошибка при перезапуске сервиса!"
    echo "📋 Последние логи:"
    journalctl -u "$SERVICE_NAME" --no-pager -n 30
    exit 1
fi

