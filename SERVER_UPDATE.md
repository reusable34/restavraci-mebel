# 📋 Инструкция по обновлению на сервере

## Вариант 1: Через Git (рекомендуется)

### Шаг 1: На вашем компьютере - загрузите файлы в репозиторий

Если у вас еще нет удаленного репозитория (GitHub, GitLab и т.д.), можно использовать простой способ:

#### Вариант А: Использовать существующий репозиторий
```bash
# На вашем компьютере
cd /Users/komp/Documents/PROGRAMIROVANIE/RestavraciMebel
git remote add origin https://github.com/ваш_username/restavraci-mebel.git
git push -u origin main
```

#### Вариант Б: Просто скопировать файлы на сервер (проще)

### Шаг 2: На сервере - инициализируйте git (если еще нет)

Выполните на сервере (`root@kolas:/opt/restavraci#`):

```bash
# Если git еще не установлен
apt-get update
apt-get install -y git

# Инициализируйте git репозиторий
cd /opt/restavraci
git init
git config user.name "Provintage"
git config user.email "nparinov03@gmail.com"

# Добавьте все файлы
git add .
git commit -m "Initial commit from server"
```

### Шаг 3: Скопируйте обновленные файлы с компьютера на сервер

**На вашем компьютере** выполните:

```bash
cd /Users/komp/Documents/PROGRAMIROVANIE/RestavraciMebel

# Создайте архив с изменениями
tar --exclude='node_modules' \
    --exclude='.next' \
    --exclude='.git' \
    --exclude='temp' \
    --exclude='photo' \
    --exclude='*.tar.gz' \
    --exclude='*.zip' \
    -czf /tmp/restavraci-update.tar.gz .

# Скопируйте на сервер (замените на ваш способ подключения)
scp /tmp/restavraci-update.tar.gz root@192.168.0.110:/tmp/
```

**На сервере** выполните:

```bash
cd /opt/restavraci

# Распакуйте архив
tar -xzf /tmp/restavraci-update.tar.gz

# Обновите зависимости и пересоберите
npm ci
npm run build

# Перезапустите сервис
systemctl restart restavraci.service

# Проверьте статус
systemctl status restavraci.service
```

---

## Вариант 2: Использовать скрипт обновления

### На сервере выполните:

```bash
cd /opt/restavraci

# Скачайте скрипт обновления (если его еще нет)
# Или создайте его вручную из файла update-on-server.sh

# Сделайте исполняемым
chmod +x update-on-server.sh

# Запустите
./update-on-server.sh
```

---

## Вариант 3: Ручное обновление конкретных файлов

Если нужно обновить только SEO файлы, скопируйте их вручную:

**На вашем компьютере:**

```bash
# Скопируйте обновленные файлы
scp app/layout.tsx root@192.168.0.110:/opt/restavraci/app/
scp app/sitemap.ts root@192.168.0.110:/opt/restavraci/app/
scp app/services/page.tsx root@192.168.0.110:/opt/restavraci/app/
scp app/portfolio/page.tsx root@192.168.0.110:/opt/restavraci/app/
scp app/contacts/layout.tsx root@192.168.0.110:/opt/restavraci/app/contacts/
scp app/page.tsx root@192.168.0.110:/opt/restavraci/app/
scp public/robots.txt root@192.168.0.110:/opt/restavraci/public/
scp components/*.tsx root@192.168.0.110:/opt/restavraci/components/
```

**На сервере:**

```bash
cd /opt/restavraci
npm run build
systemctl restart restavraci.service
```

---

## Быстрая команда (все в одном)

**На сервере выполните эту команду:**

```bash
cd /opt/restavraci && \
npm ci && \
npm run build && \
systemctl restart restavraci.service && \
systemctl status restavraci.service
```

Но сначала нужно скопировать файлы с компьютера на сервер!

---

## Проверка после обновления

После обновления проверьте:

1. **Статус сервиса:**
   ```bash
   systemctl status restavraci.service
   ```

2. **Логи (если есть ошибки):**
   ```bash
   journalctl -u restavraci.service -n 50
   ```

3. **Доступность сайта:**
   ```bash
   curl http://localhost:3000
   ```

4. **Проверка SEO мета-тегов:**
   Откройте сайт в браузере и проверьте исходный код страницы (Ctrl+U):
   - Должен быть правильный title с "Реставрация мебели в Воронеже"
   - Должны быть meta description
   - Должен быть JSON-LD с данными LocalBusiness

---

## Что было обновлено (SEO изменения):

✅ `app/layout.tsx` - SEO мета-теги, структурированные данные
✅ `app/page.tsx` - контент с упоминанием Воронежа
✅ `app/services/page.tsx` - SEO мета-теги и контент
✅ `app/portfolio/page.tsx` - SEO мета-теги
✅ `app/contacts/layout.tsx` - SEO мета-теги
✅ `app/sitemap.ts` - карта сайта
✅ `public/robots.txt` - правила для поисковых роботов
✅ `components/*.tsx` - улучшенные alt-теги изображений

Все URL обновлены на `https://provintagevrn.ru`

