# 🤖 Настройка Telegram бота для заявок

## Шаг 1: Получите токен бота

1. Откройте Telegram и найдите **@BotFather**
2. Отправьте команду `/mybots`
3. Выберите вашего бота **@Provintage_bot**
4. Выберите **API Token**
5. Скопируйте токен (выглядит как: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Шаг 2: Получите Chat ID

### Вариант А: Через getUpdates API (рекомендуется)

Выполните на вашем компьютере или на сервере:

```bash
# Замените YOUR_BOT_TOKEN на токен из шага 1
curl "https://api.telegram.org/botYOUR_BOT_TOKEN/getUpdates"
```

Затем:
1. Отправьте боту @Provintage_bot любое сообщение (например, `/start`)
2. Снова выполните команду выше
3. В ответе найдите `"chat":{"id":123456789}` - это ваш Chat ID

### Вариант Б: Через специальный бот

1. Найдите бота **@userinfobot** в Telegram
2. Отправьте ему `/start`
3. Он покажет ваш Chat ID

**Важно:** Chat ID может быть отрицательным числом (для групп) или положительным (для личных чатов).

## Шаг 3: Настройте на сервере

Выполните на сервере (`root@kolas:/opt/restavraci#`):

```bash
# Откройте .env файл
nano /opt/restavraci/.env
```

Добавьте или обновите строки:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
TELEGRAM_CHAT_ID=ваш_chat_id
CONTACT_EMAIL=provintage1404@gmail.com
```

Сохраните файл (Ctrl+O, Enter, Ctrl+X)

## Шаг 4: Перезапустите сервис

```bash
systemctl restart restavraci.service
systemctl status restavraci.service
```

## Шаг 5: Проверьте работу

1. Откройте сайт https://provintagevrn.ru
2. Заполните форму обратной связи
3. Отправьте заявку
4. Проверьте Telegram - должно прийти сообщение от бота

## Проверка логов

Если что-то не работает, проверьте логи:

```bash
journalctl -u restavraci.service -n 50 | grep -i telegram
```

## Быстрая команда для настройки

```bash
cd /opt/restavraci && \
nano .env && \
systemctl restart restavraci.service && \
systemctl status restavraci.service
```

## Формат сообщения в Telegram

Когда придет заявка, вы получите сообщение в таком формате:

```
Новая заявка с сайта Провинтаж
Имя: Иван Иванов
Телефон: +7 952 123-45-67
Сообщение: Нужна реставрация стула
```

## Устранение проблем

### Ошибка "TELEGRAM_ERROR"
- Проверьте правильность токена
- Убедитесь, что бот не заблокирован
- Проверьте, что Chat ID правильный

### Заявки не приходят
- Проверьте, что переменные окружения установлены: `cat /opt/restavraci/.env | grep TELEGRAM`
- Проверьте логи: `journalctl -u restavraci.service -f`
- Убедитесь, что бот запущен (отправьте ему `/start`)

### Бот не отвечает
- Убедитесь, что вы отправили боту `/start` перед использованием
- Проверьте, что используете правильный Chat ID

