# 🔧 Настройка Git репозитория

## Текущая ситуация

✅ Локальный git репозиторий создан  
❌ Удаленный репозиторий (GitHub/GitLab) еще не создан

---

## Вариант 1: Создать репозиторий на GitHub (рекомендуется)

### Шаг 1: Создайте репозиторий на GitHub

1. Перейдите на https://github.com
2. Нажмите кнопку **"+"** → **"New repository"**
3. Заполните:
   - **Repository name:** `restavraci-mebel` (или любое другое имя)
   - **Description:** `Сайт реставрации мебели в Воронеже - Провинтаж`
   - **Visibility:** Private (рекомендуется) или Public
   - **НЕ** ставьте галочки на "Initialize with README" и других опциях
4. Нажмите **"Create repository"**

### Шаг 2: Подключите локальный репозиторий к GitHub

После создания репозитория GitHub покажет инструкции. Выполните на вашем компьютере:

```bash
cd /Users/komp/Documents/PROGRAMIROVANIE/RestavraciMebel

# Добавьте удаленный репозиторий (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/restavraci-mebel.git

# Или если используете SSH:
# git remote add origin git@github.com:YOUR_USERNAME/restavraci-mebel.git

# Отправьте код на GitHub
git push -u origin main
```

### Шаг 3: Проверьте

Откройте https://github.com/YOUR_USERNAME/restavraci-mebel - вы должны увидеть все файлы!

---

## Вариант 2: Использовать только локальный Git (без GitHub)

Если не хотите использовать GitHub, можно работать только с локальным репозиторием.

### Для обновления на сервере:

1. **Скопируйте файлы на сервер** (через scp или deploy.sh)
2. **На сервере** инициализируйте git (если нужно):

```bash
cd /opt/restavraci
git init
git add .
git commit -m "Server version"
```

3. **Для обновления** - просто копируйте файлы заново через deploy.sh

---

## Вариант 3: Использовать GitLab или другой сервис

Процесс аналогичен GitHub:

1. Создайте репозиторий на GitLab.com
2. Выполните те же команды, заменив URL на GitLab

---

## Полезные команды Git

### Проверить статус:
```bash
git status
```

### Посмотреть историю коммитов:
```bash
git log --oneline
```

### Добавить изменения:
```bash
git add .
git commit -m "Описание изменений"
```

### Отправить на GitHub:
```bash
git push
```

### Получить изменения с GitHub:
```bash
git pull
```

---

## Текущее состояние репозитория

✅ Локальный репозиторий инициализирован  
✅ Первый коммит создан  
✅ Все SEO изменения закоммичены  
⏳ Осталось создать репозиторий на GitHub и подключить его

---

## Быстрая команда (после создания репозитория на GitHub)

```bash
cd /Users/komp/Documents/PROGRAMIROVANIE/RestavraciMebel
git remote add origin https://github.com/YOUR_USERNAME/restavraci-mebel.git
git push -u origin main
```

Замените `YOUR_USERNAME` на ваш GitHub username и `restavraci-mebel` на имя вашего репозитория.

