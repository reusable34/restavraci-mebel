# 🚀 Быстрый старт деплоя

## Шаг 1: Создайте Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot` и следуйте инструкциям
3. Сохраните токен бота (выглядит как `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)
4. Найдите свой Chat ID:
   - Отправьте `/start` вашему боту
   - Откройте в браузере: `https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates`
   - Найдите число в `"chat":{"id":123456789}` - это ваш Chat ID

## Шаг 2: Деплой на сервер

```bash
# На вашей локальной машине
cd /Users/komp/Documents/PROGRAMIROVANIE/RestavraciMebel
chmod +x deploy.sh
./deploy.sh
```

Скрипт автоматически:
- Установит Node.js если нужно
- Скопирует файлы на сервер
- Установит зависимости
- Соберет приложение
- Настроит systemd service
- Запустит приложение

## Шаг 3: Настройте переменные окружения

```bash
# Подключитесь к серверу
ssh root@192.168.0.110

# Отредактируйте .env файл
cd /opt/restavraci
nano .env
```

Заполните:
```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
TELEGRAM_CHAT_ID=ваш_chat_id
```

Сохраните (Ctrl+O, Enter, Ctrl+X) и перезапустите:
```bash
systemctl restart restavraci.service
```

## Шаг 4: Проверьте работу

```bash
# Проверьте статус
systemctl status restavraci.service

# Проверьте логи
journalctl -u restavraci.service -f

# Проверьте что приложение отвечает
curl http://localhost:3000
```

## Шаг 5: Протестируйте форму обратной связи

1. Откройте сайт в браузере
2. Заполните форму обратной связи
3. Отправьте заявку
4. Проверьте Telegram - должно прийти сообщение с заявкой

## Полезные команды

```bash
# Просмотр логов в реальном времени
journalctl -u restavraci.service -f

# Перезапуск сервиса
systemctl restart restavraci.service

# Остановка сервиса
systemctl stop restavraci.service

# Проверка статуса
systemctl status restavraci.service

# Тестирование Telegram бота (на локальной машине)
./test-telegram.sh <TOKEN> <CHAT_ID>
```

## Решение проблем

**Приложение не запускается:**
```bash
journalctl -u restavraci.service -n 50
```

**Telegram не работает:**
- Проверьте токен и Chat ID в `.env`
- Убедитесь что бот запущен (отправьте `/start`)
- Проверьте логи: `journalctl -u restavraci.service | grep TELEGRAM`

**Порт занят:**
```bash
netstat -tulpn | grep 3000
# Если занят, остановите процесс или измените PORT в .env
```

Полная документация: [DEPLOY.md](./DEPLOY.md)
